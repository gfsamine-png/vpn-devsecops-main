---
# ─── Handlers de redémarrage des services VPN ─────────────────
- name: restart strongswan
  systemd:
    name: strongswan-starter
    state: restarted

- name: restart openvpn-client
  systemd:
    name: openvpn-client@client
    state: restarted
